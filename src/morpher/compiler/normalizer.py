from morpher.ir.nodes import DesignNode


def normalize(source) -> DesignNode:
    """Convert adapter output into the shared Design IR.

    Prototype placeholder. Concrete normalization begins with Figma JSON.
    """
    raise NotImplementedError("Normalizer not implemented yet")
